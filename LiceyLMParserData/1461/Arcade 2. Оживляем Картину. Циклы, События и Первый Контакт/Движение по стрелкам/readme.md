# Движение по стрелкам

**Ограничение времени:** 20 секунд
**Ограничение памяти:** 896 Мб

Реализуйте в окне **800х600** управление белым квадратом со стороной **100**.

При запуске квадрат находится в центре окна. С помощью клавиш-стрелок его можно передвигать, однако он не должен выходить за пределы окна. Скорость перемещения задаётся параметром `velocity`.

  Шаблон кода для решения

Используйте предоставленный шаблон. Он содержит необходимую структуру для успешного прохождения тестов. Ваш код нужно написать вместо `...`

```
import arcade

SCREEN_WIDTH, SCREEN_HEIGHT = 800, 600
TITLE = "Moving Square"
VELOCITY = 20

class MyGame(arcade.Window):
    def __init__(self, width, height, title, velocity):
        super().__init__(width, height, title)
        # Сохраните переданную скорость в атрибут класса
        ...

    def setup(self):
        # Создайте атрибуты для хранения координат центра квадрата и его стороны
        ...

    def on_draw(self):
        self.clear()
        # Отрисуйте квадрат, используя его текущие координаты и размеры
        ...

    def on_key_press(self, key, modifiers):
        # 1. В зависимости от нажатой стрелки, измените координаты центра квадрата.
        # 2. После изменения, проверьте каждую из четырех границ окна.
        # 3. Если квадрат вышел за границу, скорректируйте его координату так,
        #    чтобы он касался края окна.
        ...

def setup_game(width=800, height=600, title="Moving Square", velocity=20):
    game = MyGame(width, height, title, velocity)
    game.setup()
    return game

# Блок для вашего локального тестирования (необязателен для сдачи)
def main():
    game = setup_game()
    arcade.run()

if __name__ == "__main__":
    main()
```



Результат

## Примечания
Для успешной выполнения ваше решение должно содержать функцию `setup_game` и другие методы и функции, предоставленные в шаблоне.

---
## Информация о решении

**Урок:** Arcade 2. Оживляем Картину. Циклы, События и Первый Контакт  
**Максимальный балл:** 25  
**Полученный балл:** 0  
**Статус:** new  
**Дата:** 2025-12-07T15:52:12.239319+03:00  