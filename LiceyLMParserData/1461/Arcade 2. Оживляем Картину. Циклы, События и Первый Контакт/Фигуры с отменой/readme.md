# Фигуры с отменой

**Ограничение времени:** 20 секунд
**Ограничение памяти:** 896 Мб

Создайте приложение для рисования в окне **800х600**. Пользователь должен иметь возможность нарисовать прямоугольник, зажав левую кнопку мыши в одной точке и отпустив её в другой. Во время перетаскивания мыши должен быть виден контур рисуемого прямоугольника.

Прямоугольники рисуются белыми линиями толщиной **2**. Можно нарисовать любое их количество. Сочетание клавиш `Ctrl+Z` отменяет последний нарисованный прямоугольник.

  Шаблон кода для решения

Используйте предоставленный шаблон. Он содержит необходимую структуру для успешного прохождения тестов. Ваш код нужно написать вместо `...`

```
import arcade

SCREEN_WIDTH, SCREEN_HEIGHT = 800, 600
TITLE = "Rects With Cancel"

class MyGame(arcade.Window):
    def __init__(self, width, height, title):
        super().__init__(width, height, title)

    def setup(self):
        # Список для хранения всех нарисованных прямоугольников
        self.rects = []
        # Создайте переменные для хранения параметров текущего (рисуемого)
        # прямоугольника и флаг состояния "is_drawing".
        ...

    def on_draw(self):
        self.clear()
        # Отрисуйте все сохранённые прямоугольники из self.rects.
        # Также, если пользователь сейчас рисует новый прямоугольник, отрисуйте и его.
        ...

    def on_mouse_press(self, x, y, button, modifiers):
        # Зафиксируйте начальные координаты и активируйте флаг начала рисования.
        ...

    def on_mouse_motion(self, x, y, button, modifiers):
        # Если флаг рисования активен, обновляйте ширину и высоту
        # текущего прямоугольника на основе положения мыши.
        ...

    def on_mouse_release(self, x, y, button, modifiers):
        # Если рисование было активно, добавьте новый прямоугольник в список self.rects
        # и сбросьте флаг/временные переменные.
        ...

    def on_key_press(self, key, modifiers):
        # Реализуйте отмену последнего действия: при нажатии Ctrl+Z
        # удаляйте последний элемент из списка self.rects.
        ...

def setup_game(width=800, height=600, title="Rects With Cancel"):
    game = MyGame(width, height, title)
    game.setup()
    return game

# Блок для вашего локального тестирования (необязателен для сдачи)
def main():
    setup_game()
    arcade.run()

if __name__ == "__main__":
    main()
```



Результат

## Примечания
Для успешной выполнения ваше решение должно содержать функцию `setup_game` и другие методы и функции, предоставленные в шаблоне.

---
## Информация о решении

**Урок:** Arcade 2. Оживляем Картину. Циклы, События и Первый Контакт  
**Максимальный балл:** 25  
**Полученный балл:** 25  
**Статус:** accepted  
**Дата:** 2025-12-08T08:24:42.952164+03:00  
**Контест:** 81603  
**Вердикт:** ok  