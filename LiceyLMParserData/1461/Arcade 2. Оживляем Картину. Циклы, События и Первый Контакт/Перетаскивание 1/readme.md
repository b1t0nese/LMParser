# Перетаскивание 1

**Ограничение времени:** 20 секунд
**Ограничение памяти:** 896 Мб

Напишите `arcade`-приложение в окне **800х600**, объединяющее режимы рисования и перемещения.

Сначала пользователь **рисует** один белый прямоугольник: нажатие левой кнопки мыши задаёт один его угол, а отпускание — противоположный. После того как прямоугольник нарисован, приложение **переходит в режим перемещения**. Теперь, если зажать левую кнопку мыши внутри него, его можно перетаскивать по окну.

  Шаблон кода для решения

Используйте предоставленный шаблон. Он содержит необходимую структуру для успешного прохождения тестов. Ваш код нужно написать вместо `...`

```
import arcade

SCREEN_WIDTH, SCREEN_HEIGHT = 800, 600
TITLE = "Moving Rect"

class MyGame(arcade.Window):
    def __init__(self, width, height, title):
        super().__init__(width, height, title)

    def setup(self):
        # Инициализируйте атрибуты для прямоугольника (x, y, w, h).
        # Вам понадобятся флаги для управления состояниями:
        # - rect_drawn: нарисован ли уже прямоугольник?
        # - is_drawing: рисуется ли он прямо сейчас?
        # - is_moving: перемещается ли он прямо сейчас?
        # Также нужны атрибуты для смещения курсора (delta_x, delta_y).
        ...

    def on_draw(self):
        self.clear()
        # Отрисовывайте прямоугольник, если он уже был нарисован.
        ...

    def on_mouse_press(self, x, y, button, modifiers):
        # Используйте флаг rect_drawn для выбора логики:
        # - Если ЕЩЕ НЕ нарисован: начните процесс рисования.
        # - Если УЖЕ нарисован: проверьте клик внутри и начните перемещение.
        ...

    def on_mouse_motion(self, x, y, button, modifiers):
        # В зависимости от флагов is_drawing и is_moving:
        # - Либо обновляйте размеры рисуемого прямоугольника.
        # - Либо обновляйте позицию перемещаемого прямоугольника.
        ...

    def on_mouse_release(self, x, y, button, modifiers):
        # Деактивируйте флаги is_drawing и is_moving.
        # Если рисование только что завершилось, установите флаг rect_drawn в True.
        # Здесь также можно обработать случай рисования с отрицательной шириной/высотой.
        ...

def setup_game(width=800, height=600, title="Moving Rect"):
    game = MyGame(width, height, title)
    game.setup()
    return game

# Блок для вашего локального тестирования (необязателен для сдачи)
def main():
    setup_game()
    arcade.run()

if __name__ == "__main__":
    main()
```



Результат

## Примечания
Для успешной выполнения ваше решение должно содержать функцию `setup_game` и другие методы и функции, предоставленные в шаблоне.

---
## Информация о решении

**Урок:** Arcade 2. Оживляем Картину. Циклы, События и Первый Контакт  
**Максимальный балл:** 25  
**Полученный балл:** 25  
**Статус:** accepted  
**Дата:** 2025-12-15T15:05:32.254964+03:00  
**Контест:** 81603  
**Вердикт:** ok  