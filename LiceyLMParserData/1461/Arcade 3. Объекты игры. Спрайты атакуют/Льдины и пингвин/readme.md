# Льдины и пингвин

**Ограничение времени:** 50 секунд
**Ограничение памяти:** 2048 Мб

Создайте динамическую сцену с пингвином и плавающими льдинами в окне **1000х800**.

На фоне ледяного моря ([ice_sea.png](https://yastatic.net/s3/lyceum/content/images/d2_arcade3/G_Льдины%20и%20пингвины/ice_sea.png)) в нижней половине окна плавают **5** льдин ([floe.png](https://yastatic.net/s3/lyceum/content/images/d2_arcade3/floe.png)) с масштабом **0.5**. Они движутся по горизонтали со случайными скоростями и отталкиваются от краёв. Игрок управляет пингвином с помощью стрелок. При ходьбе проигрывается анимация из 11 кадров ([penguin.zip](https://yastatic.net/s3/lyceum/content/images/d2_arcade3/penguin.zip)), которая отражается по горизонтали при смене направления.

Для решения задачи вам потребуется создать три класса: `Penguin`, `Floe` и `FaceDirection`.

Шаблон кода для решения

Используйте предоставленный шаблон. Он содержит необходимую структуру для успешного прохождения тестов. Ваш код нужно написать вместо `...`

```
import enum

import arcade
import random

# Константы
SCREEN_WIDTH = 1000
SCREEN_HEIGHT = 800
SCREEN_TITLE = "Penguin and ice floes"

FLOE_COUNT = 5
ANIMATION_SPEED = 0.1


class FaceDirection(enum.Enum):
    LEFT = 0
    RIGHT = 1


class Floe(arcade.Sprite):
    def __init__(self, filename, scale=1.0):
        super().__init__(filename, scale)
        # 1. Задайте случайную горизонтальную скорость в диапазоне от -20 до 20.
        ...

    def update(self, delta_time):
        # 1. Реализуйте горизонтальное движение с учетом скорости и delta_time.
        # 2. Реализуйте отскок от левого/правого края окна.
        #    При столкновении инвертируйте скорость и корректируйте позицию,
        #    чтобы льдина не "залипала" в стене.
        ...


class Penguin(arcade.Sprite):
    def __init__(self):
        super().__init__()
        # 1. Загрузите все 11 текстур анимации ходьбы (от item0.png до item10.png) в список.
        # 2. Установите текстуру покоя (первый кадр анимации).
        # 3. Установите начальную позицию:
        #    center_x = SCREEN_WIDTH / 2
        #    center_y = SCREEN_HEIGHT / 4
        # 4. Установите масштаб (scale) = 1.
        # 5. Установите скорость (speed) = 50.
        # 6. Инициализируйте остальные атрибуты (is_walking, face_direction, таймеры анимации и т.д.).
        ...

    def update(self, delta_time, keys_pressed):
        # 1. Реализуйте движение по 4 направлениям (W, A, S, D или стрелки).
        #    Не забудьте умножать скорость на delta_time.
        # 2. При движении по диагонали скорость должна быть скорректирована (умножена на ~0.7071).
        # 3. Проверьте границы окна, чтобы пингвин не выходил за его пределы.
        # 4. Установите правильное направление взгляда (face_direction) в зависимости от движения.
        # 5. Обновите флаг is_walking (True, если есть движение, иначе False).
        ...

    def update_animation(self, delta_time: float = 1 / 60):
        # 1. Если пингвин движется (is_walking == True):
        #    - По таймеру (с задержкой ANIMATION_SPEED) циклически переключайте кадры из списка текстур.
        #    - Отражайте текстуру по горизонтали (.flip_horizontally()) в зависимости от face_direction.
        # 2. Если пингвин стоит:
        #    - Показывайте текстуру покоя, также отражая ее в зависимости от face_direction.
        ...


class MyGame(arcade.Window):
    def __init__(self, width, height, title):
        super().__init__(width, height, title)
        # 1. Загрузите фоновую текстуру 'images/ice_sea.png'.
        # 2. Инициализируйте списки спрайтов для льдин и пингвина.
        self.floe_list = arcade.SpriteList()
        self.penguin = arcade.SpriteList()

    def setup(self):
        # 1. Создайте FLOE_COUNT (5) льдин (класс Floe).
        #    - Масштаб каждой льдины = 0.5.
        #    - Начальная позиция X: случайная в пределах ширины экрана.
        #    - Начальная позиция Y: случайная в диапазоне от 0 до 300.
        # 2. Создайте одного пингвина (класс Penguin) и добавьте в соответствующий список.
        # 3. Инициализируйте множество для отслеживания нажатых клавиш (self.keys_pressed).
        ...

    def on_draw(self):
        self.clear()
        # Отрисуйте фон и оба списка спрайтов.
        ...

    def on_update(self, delta_time):
        # Вызовите методы обновления для списка льдин и для пингвина
        # (и .update(), и .update_animation()).
        ...

    def on_key_press(self, key, modifiers):
        # Добавляйте нажатые клавиши в множество self.keys_pressed.
        ...

    def on_key_release(self, key, modifiers):
        # Удаляйте отпущенные клавиши из множества self.keys_pressed.
        ...


def setup_game(width=1000, height=800, title="Penguin and ice floes"):
    game = MyGame(width, height, title)
    game.setup()
    return game


# Блок для вашего локального тестирования (необязателен для сдачи)
def main():
    setup_game()
    arcade.run()


if __name__ == "__main__":
    main()
```

Результат

При задании констант, как в шаблоне, должен получиться результат:

## Примечания
Для успешной сдачи задачи ваше решение должно содержать функцию `setup_game` и другую сигнатуру, объявленную в предоставленном шаблоне.

Все изображения находятся в папке `images/`. Указывайте пути к файлам относительно неё (например, `images/penguins/item.png`).

---
## Информация о решении

**Урок:** Arcade 3. Объекты игры. Спрайты атакуют  
**Максимальный балл:** 33  
**Полученный балл:** 0  
**Статус:** new  
**Дата:** 2025-12-10T16:45:39.452985+03:00  