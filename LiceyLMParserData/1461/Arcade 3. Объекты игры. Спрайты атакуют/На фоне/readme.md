# На фоне

**Ограничение времени:** 20 секунд
**Ограничение памяти:** 896 Мб

Напишите программу, которая устанавливает фон для окна `arcade` из файла.

В архиве [arxiv.zip](https://yastatic.net/s3/lyceum/content/images/d2_arcade3/A_%D0%9D%D0%B0%20%D1%84%D0%BE%D0%BD%D0%B5/arxiv.zip) находится каталог `images/backgrounds` с изображениями. Имя файла для фона передаётся в параметрах при создании игры. Ваша задача — загрузить указанную текстуру и отрисовать её на всё окно размером **800х600**.

Шаблон кода для решения

Используйте предоставленный шаблон. Он содержит необходимую структуру для успешного прохождения тестов. Ваш код нужно написать вместо `...`

```
import arcade

SCREEN_WIDTH, SCREEN_HEIGHT = 800, 600
TITLE = "Texture"


class MyGame(arcade.Window):
    def __init__(self, width, height, title, filename):
        super().__init__(width, height, title)
        self.filename = filename

    def setup(self):
        # Загрузите текстуру из файла, используя полный путь
        # Сохраните загруженную текстуру в атрибут класса.
        ...

    def on_draw(self):
        self.clear()
        # Нарисуйте загруженную текстуру на весь экран.
        # Используйте arcade.draw_texture_rect.
        ...


def setup_game(width=800, height=600, title="Texture", filename='fon1.png'):
    game = MyGame(width, height, title, filename)
    game.setup()
    return game


# Блок для вашего локального тестирования (необязателен для сдачи)
def main():
    setup_game()
    arcade.run()


if __name__ == "__main__":
    main()
```

Результат

## Примечания
Для успешной сдачи задачи ваше решение должно содержать функцию `setup_game` и другую сигнатуру, объявленную в предоставленном шаблоне.

Все изображения находятся в папке `images/`. Указывайте пути к файлам относительно неё (например, `images/backgrounds/apple.png`).

---
## Информация о решении

**Урок:** Arcade 3. Объекты игры. Спрайты атакуют  
**Максимальный балл:** 20  
**Полученный балл:** 20  
**Статус:** accepted  
**Дата:** 2025-12-08T15:27:01.621947+03:00  
**Контест:** 82060  
**Вердикт:** ok  