# На старт

**Ограничение времени:** 20 секунд
**Ограничение памяти:** 896 Мб

Реализуйте в окне размером **200х200** таймер обратного отсчёта.

Отсчёт должен начинаться с **10** и останавливаться на 0. Оставшееся время отображается в виде числа красного цвета точно по центру окна. Размер шрифта задаётся параметром `font`.

  Шаблон кода для решения

Используйте предоставленный шаблон. Он содержит необходимую структуру для успешного прохождения тестов. Ваш код нужно написать вместо `...`

```
import arcade
from pyglet.graphics import Batch

SCREEN_WIDTH, SCREEN_HEIGHT = 200, 200
TITLE = "Start"
FONT = 80

class MyGame(arcade.Window):
    def __init__(self, width, height, title, font):
        super().__init__(width, height, title)
        # Сохраните переданный размер шрифта в атрибут класса
        ...

    def setup(self):
        self.batch = Batch()
        # Создайте атрибут для хранения оставшегося времени (начните с 10).
        # Запланируйте вызов метода update_timer каждую секунду с помощью arcade.schedule.
        ...

    def update_timer(self, delta_time):
        # Уменьшайте оставшееся время на 1, если оно больше 0.
        ...

    def on_draw(self):
        self.clear()
        # Создайте объект arcade.Text для отображения оставшегося времени.
        # Укажите правильные координаты, цвет, размер и центрирование (anchor).
        # Затем отрисуйте batch.
        ...

def setup_game(width=200, height=200, title="Start", font=80):
    game = MyGame(width, height, title, font)
    game.setup()
    return game

# Блок для вашего локального тестирования (необязателен для сдачи)
def main():
    game = setup_game()
    arcade.run()

if __name__ == "__main__":
    main()
```



Результат

## Примечания
Для центровки используйте параметры методы `arcade.Text`. Для успешного выполнения задачи ваше решение должно содержать функцию `setup_game` и другие методы и функции, предоставленные в шаблоне.

---
## Информация о решении

**Урок:** Arcade 2. Оживляем Картину. Циклы, События и Первый Контакт  
**Максимальный балл:** 17  
**Полученный балл:** 0  
**Статус:** new  
**Дата:** 2025-12-08T13:54:02.513086+03:00  