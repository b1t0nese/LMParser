# Масштабирование

**Ограничение времени:** 20 секунд
**Ограничение памяти:** 896 Мб

Напишите `arcade`-приложение в окне **501х501**, которое отрисовывает и масштабирует фигуру.

При запуске программа должна считывать координаты точек из файла `points.txt` и рисовать по ним белую ломаную линию толщиной **1**, отцентрованную относительно окна. При нажатии на стрелку «вверх» масштаб изображения увеличивается на **10%**, а при нажатии на «вниз» — уменьшается на **10%**.

  Шаблон кода для решения

Используйте предоставленный шаблон. Он содержит необходимую структуру для успешного прохождения тестов. Ваш код нужно написать вместо `...`

```
import arcade

SCREEN_WIDTH, SCREEN_HEIGHT = 501, 501
TITLE = "Zoom"
K = 1.1

# Код для чтения и парсинга данных из файла
with open('points.txt') as file:
    data = [list(map(lambda x: float(x.replace(',', '.')),
                     line.split(';')))
            for line in file.read().rstrip()[1:-1].split('), (')]

class MyGame(arcade.Window):
    def __init__(self, width, height, title):
        super().__init__(width, height, title)

    def setup(self):
        # Инициализируйте атрибуты для хранения центра окна
        # и текущего коэффициента масштабирования (изначально 1).
        ...

    def on_draw(self):
        self.clear()
        # Создайте новый список точек, применяя к каждой точке из 'data'
        # текущий масштаб и смещение к центру окна.
        # Затем отрисуйте этот список с помощью arcade.draw_polygon_outline.
        ...

    def on_key_press(self, key, modifiers):
        # В зависимости от нажатой стрелки, изменяйте коэффициент
        # масштабирования (умножайте или делите на K).
        ...

def setup_game(width=501, height=501, title="Zoom"):
    game = MyGame(width, height, title)
    game.setup()
    return game

# Блок для вашего локального тестирования (необязателен для сдачи)
def main():
    setup_game()
    arcade.run()

if __name__ == "__main__":
    main()
```



Результат

## Примечания
Для успешной выполнения ваше решение должно содержать функцию `setup_game` и другие методы и функции, предоставленные в шаблоне.

 Пример содержания файла points.txt

```
(2;-1), (3,5;0,5), (4;-1), (5;0), (4;2), (2;1), (2;3), (4;5), (4;6), (2;5), (1;7), (1;8), (0;7), (0;9), (-1;7), (-2;8), (-2;7), (-3;7), (-2;6), (-4;6), (-3;5), (-4;5), (-3;4), (-5;4), (-4;3), (-5;3), (-4;2), (-6;2), (-5;1), (-6;1), (-5;0), (-6;0), (-5;-1), (-6;-2), (-4;-2), (-5;-3), (-3;-4), (-4;-5), (-2;-5), (-1;-6), (3;-6), (3;-5), (1;-5), (1;-4), (2;-3), (2;-1)
```

---
## Информация о решении

**Урок:** Arcade 2. Оживляем Картину. Циклы, События и Первый Контакт  
**Максимальный балл:** 25  
**Полученный балл:** 25  
**Статус:** accepted  
**Дата:** 2026-03-01T18:26:51.426912+03:00  
**Контест:** 81603  
**Вердикт:** ok  