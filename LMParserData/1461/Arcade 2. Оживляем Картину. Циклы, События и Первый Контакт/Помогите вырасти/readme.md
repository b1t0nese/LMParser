# Помогите вырасти

**Ограничение времени:** 20 секунд
**Ограничение памяти:** 896 Мб

Реализуйте приложение на `arcade`, управляющее размером круга в окне **800х600**.

В центре окна находится круг цвета `GREEN_YELLOW` с начальным радиусом **20**. При нажатии и удержании клавиши `W` круг растёт со скоростью, заданной параметром `velocity`. При отпускании клавиши он с той же скоростью возвращается к исходному размеру и останавливается.

  Шаблон кода для решения

Используйте предоставленный шаблон. Он содержит необходимую структуру для успешного прохождения тестов. Ваш код нужно написать вместо `...`

```
import arcade

SCREEN_WIDTH, SCREEN_HEIGHT = 800, 600
TITLE = "Changing Circle"
VELOCITY = 1

class MyGame(arcade.Window):
    def __init__(self, width, height, title, velocity):
        super().__init__(width, height, title)
        # Сохраните переданную скорость в атрибут класса
        ...

    def setup(self):
        self.radius = 20
        # Создайте флаги для отслеживания состояний (рост/уменьшение)
        ...

    def on_draw(self):
        self.clear()
        # Отрисуйте круг в центре окна, используя текущий self.radius
        ...

    def on_key_press(self, key, modifiers):
        if key == arcade.key.W:
            # Установите флаги в состояние "рост"
            ...

    def on_key_release(self, key, modifiers):
        if key == arcade.key.W:
            # Установите флаги в состояние "уменьшение"
            ...

    def on_update(self, delta_time):
        # В зависимости от флагов, изменяйте self.radius.
        # Не забудьте остановить уменьшение, когда радиус достигнет 20.
        ...

def setup_game(width=800, height=600, title="Changing Circle", velocity=1):
    game = MyGame(width, height, title, velocity)
    game.setup()
    return game

# Блок для вашего локального тестирования (необязателен для сдачи)
def main():
    game = setup_game()
    arcade.run()

if __name__ == "__main__":
    main()
```



Результат

## Примечания
Для успешной выполнения ваше решение должно содержать функцию `setup_game` и другие методы и функции, предоставленные в шаблоне.

---
## Информация о решении

**Урок:** Arcade 2. Оживляем Картину. Циклы, События и Первый Контакт  
**Максимальный балл:** 17  
**Полученный балл:** 17  
**Статус:** accepted  
**Дата:** 2025-12-08T13:53:58.523163+03:00  
**Контест:** 81603  
**Вердикт:** ok  