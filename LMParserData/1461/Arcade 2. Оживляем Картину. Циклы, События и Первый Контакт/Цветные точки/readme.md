# Цветные точки

**Ограничение времени:** 20 секунд
**Ограничение памяти:** 512 Мб

Реализуйте приложение на `arcade`, которое на окне размером **800х600** по клику левой кнопкой мыши рисует в месте клика кружок радиусом **20**.

Цвета для кругов должны циклически браться из списка 16-ричных строк, передаваемого в параметрах при создании игры.

  Шаблон кода для решения

Используйте предоставленный шаблон. Он содержит необходимую структуру для успешного прохождения тестов. Ваш код нужно написать вместо `...`

```
import arcade
from arcade.types import Color

SCREEN_WIDTH = 800
SCREEN_HEIGHT = 600
TITLE = "Color points"
COLORS = ['#ffc000', '#dc00a9', '#0065ac', '#22ad00']

class MyGame(arcade.Window):
    def __init__(self, width, height, title, colors):
        super().__init__(width, height, title)
        # Сохраните переданные цвета в атрибут класса
        ...

    def setup(self):
        self.points = []
        self.radius = 20

    def on_draw(self):
        self.clear()
        # В цикле отрисуйте все круги из self.points,
        # чередуя цвета из сохраненного списка
        ...

    def on_mouse_press(self, x, y, button, modifiers):
        # Добавьте координаты (x, y) в self.points
        ...

def setup_game(width=800, height=600, title="Color points", colors=None):
    # Если в функцию не передали список цветов, используйте COLORS по умолчанию
    game = MyGame(width, height, title, COLORS[:] if colors is None else colors)
    game.setup()
    return game

# Блок для вашего локального тестирования (необязателен для сдачи)
def main():
    game = setup_game()
    arcade.run()

if __name__ == "__main__":
    main()
```



Результат

## Примечания
Для успешной выполнения ваше решение должно содержать функцию `setup_game` и другие методы и функции, предоставленные в шаблоне.

---
## Информация о решении

**Урок:** Arcade 2. Оживляем Картину. Циклы, События и Первый Контакт  
**Максимальный балл:** 16  
**Полученный балл:** 16  
**Статус:** accepted  
**Дата:** 2025-12-04T15:40:48.492913+03:00  
**Контест:** 81603  
**Вердикт:** ok  