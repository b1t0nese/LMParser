# Перетаскивание 0

**Ограничение времени:** 20 секунд
**Ограничение памяти:** 896 Мб

Реализуйте приложение в окне **800х600**, демонстрирующее перетаскивание объекта.

При запуске в центре окна находится белый прямоугольник размером **400х300**. Если зажать левую кнопку мыши внутри него, прямоугольник можно перемещать. Важно, чтобы при движении сохранялось относительное положение курсора и точки захвата на прямоугольнике. При отпускании кнопки он остаётся на новом месте.

  Шаблон кода для решения

Используйте предоставленный шаблон. Он содержит необходимую структуру для успешного прохождения тестов. Ваш код нужно написать вместо `...`

```
import arcade

SCREEN_WIDTH, SCREEN_HEIGHT = 800, 600
TITLE = "Moving Rect"

class MyGame(arcade.Window):
    def __init__(self, width, height, title):
        super().__init__(width, height, title)

    def setup(self):
        # Инициализируйте атрибуты для хранения позиции, размеров прямоугольника,
        # флага перемещения (is_moving) и смещения курсора (delta_x, delta_y).
        ...

    def on_draw(self):
        self.clear()
        # Отрисуйте прямоугольник, используя его текущие атрибуты.
        ...

    def on_mouse_press(self, x, y, button, modifiers):
        # Проверьте, был ли клик внутри прямоугольника.
        # Если да, вычислите смещение курсора относительно угла прямоугольника,
        # сохраните его и активируйте флаг перемещения.
        ...

    def on_mouse_motion(self, x, y, button, modifiers):
        # Если флаг перемещения активен, обновляйте позицию прямоугольника,
        # используя текущие координаты мыши и сохранённое смещение.
        ...

    def on_mouse_release(self, x, y, button, modifiers):
        # Деактивируйте флаг перемещения.
        ...

def setup_game(width=800, height=600, title="Moving Rect"):
    game = MyGame(width, height, title)
    game.setup()
    return game

# Блок для вашего локального тестирования (необязателен для сдачи)
def main():
    setup_game()
    arcade.run()

if __name__ == "__main__":
    main()
```



Результат

## Примечания
Для успешной выполнения ваше решение должно содержать функцию `setup_game` и другие методы и функции, предоставленные в шаблоне.

---
## Информация о решении

**Урок:** Arcade 2. Оживляем Картину. Циклы, События и Первый Контакт  
**Максимальный балл:** 25  
**Полученный балл:** 25  
**Статус:** accepted  
**Дата:** 2025-12-08T09:40:22.931555+03:00  
**Контест:** 81603  
**Вердикт:** ok  