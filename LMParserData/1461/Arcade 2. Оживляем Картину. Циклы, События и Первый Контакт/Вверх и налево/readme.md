# Вверх и налево

**Ограничение времени:** 20 секунд
**Ограничение памяти:** 896 Мб

Реализуйте приложение на `arcade`, в котором по клику левой кнопкой мыши появляются белые шарики радиусом **20**.

Сразу после появления шарик начинает двигаться вверх и влево под углом **45 градусов**. При столкновении со стеной направление его скорости по соответствующей оси меняется на противоположное. Скорость движения задаётся параметром `velocity`.

  Шаблон кода для решения

Используйте предоставленный шаблон. Он содержит необходимую структуру для успешного прохождения тестов. Ваш код нужно написать вместо `...`

```
import arcade

SCREEN_WIDTH, SCREEN_HEIGHT = 800, 600
TITLE = "Drop balls"
VELOCITY = 2

class MyGame(arcade.Window):
    def __init__(self, width, height, title, velocity):
        super().__init__(width, height, title)
        # Сохраните переданную скорость в атрибут класса
        ...

    def setup(self):
        # Список для хранения координат [x, y] каждого шарика
        self.points = []
        # Параллельный список для хранения скоростей [dx, dy] каждого шарика
        self.speed = []
        # Задайте радиус шариков
        ...

    def on_draw(self):
        self.clear()
        # В цикле для каждого шарика:
        # 1. Проверьте столкновение со стенами и инвертируйте скорость по нужной оси.
        # 2. Обновите координаты шарика, используя его скорость.
        # 3. Отрисуйте шарик в новых координатах.
        ...

    def on_mouse_press(self, x, y, button, modifiers):
        # При клике добавьте в self.points координаты нового шарика,
        # а в self.speed — его начальную скорость.
        ...

def setup_game(width=800, height=600, title="Drop balls", velocity=2):
    game = MyGame(width, height, title, velocity)
    game.setup()
    return game

# Блок для вашего локального тестирования (необязателен для сдачи)
def main():
    game = setup_game()
    arcade.run()

if __name__ == "__main__":
    main()
```



Результат

## Примечания
Для успешной выполнения ваше решение должно содержать функцию `setup_game` и другие методы и функции, предоставленные в шаблоне.

---
## Информация о решении

**Урок:** Arcade 2. Оживляем Картину. Циклы, События и Первый Контакт  
**Максимальный балл:** 17  
**Полученный балл:** 17  
**Статус:** accepted  
**Дата:** 2026-03-01T18:12:48.913181+03:00  
**Контест:** 81603  
**Вердикт:** ok  