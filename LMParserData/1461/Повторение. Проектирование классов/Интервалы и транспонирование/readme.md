# Интервалы и транспонирование

**Ограничение времени:** 1 секунда
**Ограничение памяти:** 64Mb

Добавим в класс `N`ote сравнение нот. Сравнивать будем по высоте, без учёта длительности, причём
      будем считать большей ту ноту, чей тон выше (частота колебаний больше).

 Также добавим возможность переноса звука
      выше или ниже. У нас получится упрощённая версия транспонирования, так как на нашем ксилофоне есть только «белые» клавиши.
      Перенос будет происходить с помощью сдвигов

      >


      >


      >>


>> вверх и

      <


      <


      <<


<< вниз на гарантированно целое положительное значение. Например, сдвиг
      `«`до» на три вправо (

      >


      >


      3


      >> 3


>>3) приводит к `«`фа».

 Сдвиг должен работать циклически: если сдвинуть ту же `«`до» на один вниз (

      <


      <


      1


      << 1


<<1), должна получиться `«`си».
      А если сдвинуть `«`си» на один выше (

      >


      >


      1


      >> 1


>>1) — опять `«`до».

 И ещё один новый метод — `g`et_interval, позволяющий получить значение интервала между двумя
      нотами: `«`прима», `«`секунда», `«`терция», `«`кварта»,
      `«`квинта», `«`секста» или `«`септима», где `«`прима»
      — интервал между двумя равными по тону нотами, а `«`септима» — максимальный доступный на нашем ксилофоне
      интервал — между `«`до» и `«`си».

 В решении нужно объявить и можно
      использовать следующие константы (тестирующая система будет импортировать их из вашего кода):

```
N = 7
      PITCHES = ["до", "ре", "ми", "фа", "соль", "ля", "си"]
      LONG_PITCHES = ["до-о", "ре-э", "ми-и", "фа-а", "со-оль", "ля-а", "си-и"]
      INTERVALS = ["прима", "секунда", "терция", "кварта", "квинта", "секста", "септима"]
```

## Формат ввода
Каждый тест представляет собой код, в котором будет использоваться ваш класс.

## Примеры
### Пример 1
**Ввод:**
```
from solution import (
    Note, N, PITCHES,
    LONG_PITCHES, INTERVALS
)

fa1 = Note("фа", True)
fa2 = Note("фа")
print(fa1 == fa2)
print(fa1 > fa2)
print(fa1 < fa2)
print(fa1 <= fa2)

la = Note("ля", True)
print(fa1 < la)
```
**Вывод:**
```
True
False
False
True
True
```

## Примечания
Несмотря на то, что для правильной работы класса не обязательно реализовывать все шесть операций сравнения, [рекомендуется](https://www.python.org/dev/peps/pep-0008/#programming-recommendations)
      всё же [делать это](https://docs.python.org/3/reference/datamodel.html#object.__lt__) (либо использовать [functools.total_ordering()](https://docs.python.org/3/library/functools.html#functools.total_ordering))

---
## Информация о решении

**Урок:** Повторение. Проектирование классов  
**Максимальный балл:** 33  
**Полученный балл:** 33  
**Статус:** accepted  
**Дата:** 2025-09-23T15:28:08.132390+03:00  
**Контест:** 8640  
**Вердикт:** ok  